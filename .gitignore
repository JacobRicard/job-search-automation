# Secrets
.env
*.env

# macOS
.DS_Store

# Node
node_modules/

# Python
.venv/
__pycache__/

# SQLite
jobs.db
*.db-shm
*.db-wal

# Generated files
*.pdf
jobs.json
transcript.md
public/bookmarklet.js

# Personal data — ignored so the public template stays clean.
# After running /setup, use the generated environment setup script to restore
# files in each cloud session (one-time paste into Claude Code environment settings).
/.context/
/data/

# Local backups (from scripts/backup.sh)
/backups/

# Logs
logs/

# Cache files (company targeting, ATS slug health) are per-user
market-research-cache.json
slug-health.json
jd-health.json
slug-health-dismissed.json

# Deploy automation stays local (never ship iMac infra details publicly)
.github/workflows/

# Claude user-level skill overrides stay local (may contain personal paths)
.claude/skills/
# Claude local settings stay local (may contain personal tool permissions)
.claude/settings.local.json

.aider*
