'use strict';

const { sleep } = require('../utils');

const DELAYS = {
  KEY_PRESS: 150,
  KEY_CONFIRM: 250,
  INPUT_SETTLE: 500,
  DOM_UPDATE: 800,
  PAGE_SETTLE: 1000,
  AUTOCOMPLETE: 1200,
  REACT_RENDER: 2000,
  UPLOAD_PROCESS: 3000,
  SUBMIT_NAV: 4000,
  SECURITY_CODE: 5000,
  SUBMIT_COMPLETE: 6000,
  SECURITY_SUBMIT: 8000,
};

module.exports = { sleep, DELAYS };
