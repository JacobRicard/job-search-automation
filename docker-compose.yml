services:
  dashboard:
    build: .
    ports:
      - "${DASHBOARD_PORT:-3131}:${DASHBOARD_PORT:-3131}"
    volumes:
      - ./data:/app/data
      - job_search_db:/app/db
      - ./logs:/app/logs
      - ./.env:/app/.env
    env_file: .env
    environment:
      DATA_DIR: /app/data
      DB_DIR: /app/db
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - |
          const port = process.env.DASHBOARD_PORT || 3131;
          require('http').get('http://localhost:' + port + '/healthz', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1));
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  worker:
    build: .
    volumes:
      - ./data:/app/data
      - job_search_db:/app/db
      - ./logs:/app/logs
      - ./.env:/app/.env
    env_file: .env
    environment:
      DATA_DIR: /app/data
      DB_DIR: /app/db
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      dashboard:
        condition: service_healthy
    command: ["/bin/sh", "scripts/docker-worker.sh"]
    restart: unless-stopped

volumes:
  job_search_db:
